<p align="center">
  <img src="public/logo.svg" alt="Uni-X Logo" width="300" />
</p>

<h1 align="center">Uni-X UserScript v3.0</h1>

[![JavaScript](https://img.shields.io/badge/JavaScript-ES6%2B-yellow.svg)](https://developer.mozilla.org/en-US/docs/Web/JavaScript)
[![Tampermonkey](https://img.shields.io/badge/Manager-Tampermonkey_BETA-004838.svg)](https://www.tampermonkey.net/)
[![Security Research](https://img.shields.io/badge/Research-Reverse_Engineering-red.svg)](https://owasp.org/www-community/vulnerabilities/Insecure_Direct_Object_References)
[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](https://opensource.org/licenses/MIT)

> **Uni-X UserScript** — это модульное решение, работающее непосредственно в браузере. Скрипт декомпозирует ограничения интерфейса, позволяя копировать защищенный контент, симулировать активность вкладки и автоматизировать прохождение видео-лекций за счет перехвата и репликации токенов просмотра.

---

### 🛡️ Security Analysis: Анатомия уязвимости

#### 🕵️ Discovery Methodology: Reverse Engineering
Данная уязвимость была выявлена в ходе **Reverse Engineering** (обратного проектирования) клиентской логики приложения и динамического анализа сетевого трафика.

В процессе исследования взаимодействия Frontend <-> Backend было установлено:
1.  **Traffic Analysis:** Анализ XHR/Fetch запросов показал, что payload, отправляемый на сервер для подтверждения просмотра, содержит подписанный токен.
2.  **Storage Inspection:** Изучение `localStorage` выявило, что состояние плеера и токены кэшируются на клиенте в незашифрованном виде.
3.  **Black-box Testing:** Путем подмены параметров запроса (Request Tampering) было экспериментально доказано, что сервер не валидирует семантическую связь между токеном и ID урока.

---

#### 1. Механизм уязвимости (The Mechanism)
Платформа использует подписанные JWT-токены (или аналогичные структуры) для подтверждения прогресса. Эксплуатируемый вектор атаки классифицируется как **Replay Attack** (Атака повторного воспроизведения) в сочетании с **Insecure Direct Object References (IDOR)**.

*   **Ожидаемое поведение:** Токен должен быть уникальным криптографическим подтверждением для пары `User + LessonID`.
*   **Реальность:** Токен подтверждает лишь факт активности пользователя, но архитектурно отвязан от контекста конкретного ресурса.

#### 2. Why It Works (Root Cause)
Бэкенд-сервер совершает критическую ошибку при валидации входящего POST-запроса на endpoint `/api/lessons/{id}/watched`.

Процесс валидации на сервере выглядит следующим образом:
1.  ✅ **Check Signature:** Сервер проверяет цифровую подпись токена. Она валидна, так как токен был реально выдан сервером ранее.
2.  ✅ **Check User:** Сервер проверяет принадлежность токена пользователю.
3.  ❌ **Missing Context Check:** Сервер **НЕ сверяет** ID урока, зашитого внутри токена (Scope), с ID урока, который передается в URL запроса.

> **Результат:** Скрипт извлекает валидный "слепок" (токен) от Урока №1 и инъектирует его в запрос для Урока №55. Сервер видит валидную подпись и одобряет запрос, игнорируя подмену контекста.

---

### 🔧 Mitigation Strategies (Как это исправить?)

Для разработчиков платформы (Backend Recommendations). Чтобы закрыть данную уязвимость, необходимо внедрить **Context Binding** и **Strict Scope Validation**.

#### Шаг 1: Привязка к контексту (Payload Binding)
При генерации токена необходимо включать `lesson_id` в payload и подписывать его.

**Insecure Payload (Current):**
```json
{
  "sub": "12345", // User ID
  "role": "student",
  "iat": 1700000000
}
```

**Secure Payload (Recommended):**
```json
{
  "sub": "12345",
  "scope": {
     "target_entity": "lesson",
     "target_id": "1055" // <--- ID конкретного урока
  },
  "exp": 1700000300, // Short TTL (5 min)
  "jti": "unique-nonce-id" // Protection against double-submit
}
```

#### Шаг 2: Строгая валидация (Strict Validation)
На бэкенде при обработке запроса `POST /api/lessons/1055/watched`:

```python
def validate_view(request, lesson_id):
    token = request.token
    
    # 1. Проверка подписи
    if not token.is_valid_signature(): raise AuthError()
    
    # 2. ПРОВЕРКА КОНТЕКСТА (Critical Fix)
    if token.payload.scope.target_id != lesson_id:
        raise SecurityError("Token Scope Mismatch: Token is for lesson X, but request is for lesson Y")
        
    # 3. Проверка срока действия
    if token.expired(): raise AuthError()
```

---

### 🎥 Демонстрация работы

<p align="center">

https://github.com/user-attachments/assets/8273ccc1-0de5-4246-8393-30539d5170e2

</p>

---

## Установка и Настройка (Важно!)

Для корректной работы перехватчиков требуется **Tampermonkey BETA** и специфичные настройки прав доступа.

### Шаг 1. Установка менеджера
Используйте **только Beta-версию**, так как она поддерживает более глубокий перехват API (XHR hooking) и работы с LocalStorage.

- [Chrome / Edge / Brave (Tampermonkey BETA)](https://chrome.google.com/webstore/detail/tampermonkey-beta/gcalenpjmijncebpfijmoaglllgpjagf)
- [Firefox (Tampermonkey)](https://addons.mozilla.org/en-US/firefox/addon/tampermonkey/)

### Шаг 2. Настройка прав доступа
После установки расширения необходимо разрешить ему инъекцию скриптов в защищенные контексты:

1.  Откройте меню управления расширениями (`chrome://extensions/`).
2.  Найдите карточку **Tampermonkey BETA**.
3.  Нажмите кнопку **"Сведения"** (Details).
4.  Активируйте галочку: **"Разрешить открывать файлы по ссылкам"** (Allow access to file URLs) или **"Разрешить пользовательские скрипты"**.
5.  *Без этого пункта модуль "Video Sniffer" не сможет извлечь токен.*

### Шаг 3. Инсталляция
Нажмите кнопку ниже для установки последней версии.

[![Install Script](https://img.shields.io/badge/Install-Click_Here-success?style=for-the-badge&logo=tampermonkey)](https://github.com/I3eka/uni-x-userscript/raw/main/uni-x-full.user.js)

---

## Руководство по использованию

### 🎓 Авто-просмотр лекций

**Первый запуск (Калибровка):**
Скрипту необходим валидный токен ("цифровая подпись") вашего пользователя.
1.  Откройте любое непросмотренное видео.
2.  Посмотрите его до конца вручную (один раз).
3.  Скрипт перехватит токен и сохранит его. Вы увидите уведомление: `🎬 Новый токен просмотра сохранен!`.

**Автоматический режим:**
После калибровки просто открывайте следующие лекции. Скрипт автоматически:
1.  Обнаружит, что видео не просмотрено.
2.  Сформирует запрос, используя сохраненный токен и ID текущего урока.
3.  Отправит его на сервер. Страница перезагрузится с зеленой галочкой.

### 📋 Smart Copy (Тесты)

1.  Наведите курсор на область вопроса.
2.  Блок подсветится **зеленой рамкой** (`#50C878`).
3.  Кликните в любом месте внутри рамки.
4.  Скрипт скопирует очищенный текст вопроса и вариантов ответов в буфер обмена.

---

## Disclaimer

Данный проект разработан в **образовательных и исследовательских целях (Security Research & PoC)**.

1.  **Ответственность:** Автор не несет ответственности за любые последствия использования данного ПО, включая блокировку учетных записей.
2.  **Назначение:** Проект демонстрирует уязвимости в архитектуре веб-приложений и методы взаимодействия с DOM/API на стороне клиента.
3.  **Use wisely:** Используйте данный инструмент только в рамках этических норм и правил вашего учебного заведения.

## License

MIT